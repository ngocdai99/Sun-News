import React from 'react';
import {View, Text} from 'react-native';
import {TitleText} from '~/components';

const ExploreScreen: React.FC = () => {
  return (
    <View style={{flex: 1}}>
      <TitleText text="Explore Screen" />
    </View>
  );
};
export default ExploreScreen
